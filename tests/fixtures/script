echo $(
  # comment
  echo "hello" \
    "world
    adding $(echo "\"stuff") \"just to see
    how it behaves" | # comment
      cat | ( # comment
        # comment
        cat -n # comment
      ) | cat && # comment
      echo bye
)
